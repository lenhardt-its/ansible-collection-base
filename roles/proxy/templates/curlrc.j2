{{ ansible_managed | comment }}

# System-wide curl configuration file for proxy settings

# Use a proxy server
{% if proxy_env.http_proxy is defined and proxy_env.http_proxy != '' %}proxy = "{{ proxy_env.http_proxy }}"{% endif %}

# Bypass proxy for local addresses
{% if proxy_env.no_proxy is defined and proxy_env.no_proxy != '' %}noproxy = "{{ proxy_env.no_proxy }}"{% endif %}
